<div class='container container__login'>
	<div class='card'>
		<h1 class='card-title'>Вход</h1>
		<div class='elements-wrap__column'>
			{{> TextField title='логин' type='text' class='input__primary' name='login' }}
			{{> TextField title='пароль' type='text' class='input__primary' name='password' }}
			{{> Button title='войти' filled='true'}}
			{{! +TextField('логин','text','input__primary','login')
					+TextField('пароль','password','input__primary','password')
				.elements-wrap__column
					+Button('Войти', 'btn btn-primary__filled')
					a(href='Registration.pug', class='link') Зарегистрироваться }}
		</div>
	</div>
	<div>{{> SwitcherPages  }}</div>
</div>